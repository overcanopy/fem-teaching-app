services:
  - type: web
    name: fem-teaching-backend
    env: python
    region: oregon   # or 'frankfurt' / 'singapore' depending on your location
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend:app --host 0.0.0.0 --port 10000
    autoDeploy: true
